## Array
- 长度固定，占用一段连续的内存空间，存储多个**同类型**的值
- 声明
    - Note: 如果在声明数组时未初始化数组的值，那么其元素值将是不确定的 —— 意味着访问元素获取的值是之前驻留在该**内存单元**中的值。
```c++
int months[12];  // 声明一个包含12个整数的数组

// 可以在声明时初始化数组
int yamcosts[3] = {20, 30, 5};

// 注意
```

- sizeof 运算符获得的是数组的字节数


## Vector

